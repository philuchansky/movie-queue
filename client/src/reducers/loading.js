const initialState = {
  currentUser: false,
  queue: false,
  featuredMovies: false,
  movie: false,
  searchResults: false
}

export default (state = initialState, action) => {
  switch(action.type) {
    default: return state
  }
}